# Database Configuration
# The application supports both SQLite (local dev) and PostgreSQL (production/Docker)
# Provider is set in prisma/schema.prisma

# OPTION 1: SQLite (default for local development - simplest)
# No services needed, just a local database file
DATABASE_URL="file:./prisma/dev.db"

# OPTION 2: PostgreSQL locally via Docker (better dev/prod parity)
# Requires: docker-compose up -d
# Also: Change provider in prisma/schema.prisma from "sqlite" to "postgresql"
# DATABASE_URL="postgresql://cashlines:cashlines_secure_password@localhost:5432/cashlines?schema=public"

# OPTION 3: PostgreSQL in container (running app inside Docker)
# Change provider in prisma/schema.prisma from "sqlite" to "postgresql"
# DATABASE_URL="postgresql://cashlines:cashlines_secure_password@postgres:5432/cashlines?schema=public"

# Next.js Configuration
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Note: Copy this file to .env for local development
# $ cp .env.example .env

# Next.js/Auth secrets (add when implementing auth)
# NEXTAUTH_SECRET=your-secret-key-here
# NEXTAUTH_URL=http://localhost:3000
