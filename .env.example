# Database Configuration (PostgreSQL)
# 
# Development and production both use PostgreSQL.
# For local development, start PostgreSQL with:
#   docker-compose up -d
#
# Then run migrations:
#   npx prisma db push
#   npx prisma generate
#

DATABASE_URL=postgresql://cashlines:cashlines_secure_password@localhost:5432/cashlines?schema=public

# App Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: Copy this file to .env for local development
# $ cp .env.example .env
