# Database Configuration
# 
# SCHEMA DEFAULT: SQLite (provider = "sqlite" in prisma/schema.prisma)
# DATABASE_URL determines the actual database connection:
#
# LOCAL DEVELOPMENT (default):
#   DATABASE_URL=file:./prisma/dev.db
#   No services needed
#
# DOCKER-COMPOSE (PostgreSQL):
#   The docker-compose.yml environment already sets:
#   DATABASE_URL=postgresql://cashlines:cashlines_secure_password@postgres:5432/cashlines?schema=public
#   This overrides .env when using docker-compose
#
# STANDALONE DOCKER (SQLite):
#   Set DATABASE_URL=file:./prisma/dev.db
#   Same as local development
#

DATABASE_URL=file:./prisma/dev.db

# App Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: Copy this file to .env for local development
# $ cp .env.example .env
