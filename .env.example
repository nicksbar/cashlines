# Database Configuration
# Default: PostgreSQL (for Docker and production)
# Alternative: SQLite (for local development only)

# PostgreSQL (Docker/production - requires docker-compose or external PostgreSQL)
# DATABASE_URL="postgresql://cashlines:cashlines_secure_password@postgres:5432/cashlines?schema=public"

# ALTERNATIVE: SQLite for local development only
# To use SQLite:
#   1. Uncomment the line below
#   2. Comment out the PostgreSQL line above
#   3. Change provider in prisma/schema.prisma from "postgresql" to "sqlite"
#   4. Run: npx prisma db push
# Database Configuration
# Default: SQLite for simplicity (no services needed)

DATABASE_URL=file:./prisma/dev.db

# For docker-compose with PostgreSQL:
# 1. Uncomment the postgresql provider lines in prisma/schema.prisma
# 2. Comment out the sqlite provider line in prisma/schema.prisma
# 3. Change DATABASE_URL to:
#    DATABASE_URL=postgresql://cashlines:cashlines_secure_password@postgres:5432/cashlines?schema=public
# 4. Run: docker-compose up -d

NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: Copy this file to .env for local development
# $ cp .env.example .env

# Next.js/Auth secrets (add when implementing auth)
# NEXTAUTH_SECRET=your-secret-key-here
# NEXTAUTH_URL=http://localhost:3000
