version: '3.8'

services:
  # PostgreSQL database (production-recommended)
  postgres:
    image: postgres:16-alpine
    container_name: cashlines-postgres
    environment:
      POSTGRES_USER: cashlines
      POSTGRES_PASSWORD: cashlines_secure_password
      POSTGRES_DB: cashlines
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cashlines"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cashlines-network
    # Security: don't expose to host network in production
    # Use networks instead for inter-container communication

  # Cashlines web app (uses PostgreSQL by default)
  cashlines:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cashlines
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://cashlines:cashlines_secure_password@postgres:5432/cashlines?schema=public
      - HOSTNAME=0.0.0.0
      - PORT=3000
    volumes:
      - cashlines-logs:/app/logs
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cashlines-network
    # Remove 'build' and uncomment 'image' for pre-built images:
    # image: cashlines:latest

volumes:
  postgres-data:
    driver: local
  cashlines-logs:
    driver: local

networks:
  cashlines-network:
    driver: bridge
